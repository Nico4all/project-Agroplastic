{
  "name": "personal-finance-monolith",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run start:dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "copy:frontend": "node scripts/copy-frontend.js",
    "build:monolith": "npm run build --prefix frontend && npm run copy:frontend && npm run build --prefix backend",
    "start:prod": "npm run start:prod --prefix backend",
    "db:migrate": "npm run prisma:migrate --prefix backend",
    "db:generate": "npm run prisma:generate --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
